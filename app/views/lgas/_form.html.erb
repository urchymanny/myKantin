<%= form_for(lga) do |f| %>
  <% if lga.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lga.errors.count, "error") %> prohibited this lga from being saved:</h2>

      <ul>
      <% lga.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :state_id %>
    <%= f.number_field :state_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

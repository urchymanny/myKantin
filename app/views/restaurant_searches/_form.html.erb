<%= form_for(restaurant_search) do |f| %>
  <% if restaurant_search.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant_search.errors.count, "error") %> prohibited this restaurant_search from being saved:</h2>

      <ul>
      <% restaurant_search.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :keywords %>
    <%= f.text_field :keywords %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
